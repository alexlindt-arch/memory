{
  "name": "memory",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Memory card game with four themes, three board sizes and two players.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.80.0",
    "typescript": "^5.6.0",
    "vite": "^8.2.1"
  }
}
